<!DOCTYPE html>
<html>
    <head>
        <title>All Songs</title>
    </head>
    <body>
    <h1>All Songs</h1>
    <% @songs.each do |song| %>
        <h2><%= link_to "#{song.artist_name} - #{song.title}", song_path(song) %></h2>
    <% end %>
    </body>
</html>