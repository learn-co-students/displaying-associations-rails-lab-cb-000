<h1> Artists </h1>
<ul>
  <% @artists.each do |artist| %>
    <li>
      <h3> <%= pluralize(artist.songs.count, 'song') %> </h3>
      <%= link_to artist.name, artist_path(artist) %>
    </li>
  <% end %>
</ul>
