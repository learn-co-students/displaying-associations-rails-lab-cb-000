<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <% @artists.each do |artist| %>
      <%= link_to "Artist Name: #{artist.name}  Song Count: #{pluralize(artist.song_count, 'song')}", artist_path(artist)%>
    <% end %>
  </body>
</html>
